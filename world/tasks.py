from typing import Dict, Any

KEY_DOOR_TASK: Dict[str, Any] = {
    "name": "key_door_task",
    "goal": "Find the key, then go to the door, open it, and reach the goal tile behind it.",
    "max_steps": 40,
}
